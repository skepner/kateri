name: kateri
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.2 <3.0.0'

executables:
  kateri: kateri

dev_dependencies:
  lints: ^1.0.0
  flutter_lints: ^1.0.0

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.7.3
  path_provider: ^2.0.9
  intl: ^0.17.0
  universal_platform: ^1.0.0+1
  typed_data: ^1.3.0

  # open ace file
  file_picker: ^4.5.1
  # file_selector_macos: ^0.0.4+1
  # file_selector: ^0.8.2+1
  # file_selector_web: ^0.8.1+3

  # exportPdf
  file_saver: ^0.1.0
  brotli: ^0.5.0

flutter:
  uses-material-design: true
